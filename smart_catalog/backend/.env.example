# Flask 密钥（生产环境请替换为随机字符串）
SECRET_KEY=intellicat-secret-key

# SQLite 数据库路径（可改为 MySQL/PostgreSQL 连接串）
# DATABASE_URL=sqlite:///intellicat.db

# 外部匹配 API 地址（不设置则使用内置随机匹配占位逻辑）
# MATCH_API_URL=https://your-api.example.com/match

# Flask 主机和端口（命令行覆盖更方便）
# FLASK_HOST=0.0.0.0
# FLASK_PORT=5000
